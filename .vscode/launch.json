{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "gdb",
            "request": "launch",
            "target": "${workspaceRoot}/target/debug/perm",
            "cwd": "${workspaceRoot}",
            "arguments": "data/config.json data/output.csv"
        }
    ]
}